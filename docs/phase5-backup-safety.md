# 第五阶段分期

5A：备份安全预览。5B：清理任务和重试记录。5C：按需读取、真正分页、性能测量和 CI 浏览器测试。按最新约定，三部分统一在 `feat/phase5-performance` 一个 Preview 中验收，不合并 main。

## 5A 已实现

- 原导出改为完整路径、版本2 JSON/CSV/校验清单、SHA256。
- 可见阶段进度，完整/部分文件名，缺图清单和当前页面补下载。
- 本地 ZIP 校验、同谷仓差异预览，仅只读；不提供覆盖按钮。
- 包含当前账号权限可读取的业务数据和关联图片（含软删除图），不含平台配置、Auth 密码或存储孤儿文件。
- 多请求读取不是事务快照，请在无编辑期间导出。手机内存有限：媒体200MB、输入ZIP250MB、解压300MB限额；更大使用原本机完整备份流程。
- 生成下载不代表浏览器已保存，部分备份不写成功导出记录。

## 尚不宣称完成

- 旧版 ZIP 无校验清单，拒绝作为本轮恢复预览来源；旧备份仍保留。
- 本轮恢复预览不是实际数据库恢复按钮。跨表事务快照与更大文件流式导出仍不在浏览器预览内，不应宣称第五阶段全部完成。
- 不自动删除旧图，不清理正式库，不修改 main。

## 校验缺陷修复与回归（2026-09-06）

- 导出与导入都要求版本2的15张必需表，拒绝缺表、未知表、非法行、缺失/重复标识、跨谷仓记录及缺失业务关联。
- 拒绝异常图片路径、ZIP规范化前的穿越路径、未声明文件、缺少CSV校验项、不一致的缺图清单和非布尔完整标志。
- 修复CSV公式转义字符类，正常文字不再误加单引号。
- 34项自动化测试通过；TypeScript、变更文件ESLint与生产构建通过。
- 本地隔离服务+Chrome，390px/1280px：正常备份下载与再次导入、缺表ZIP错误提示、错误后重新导入成功，全部通过。数据库前后相同，无页面异常。
- 以上不是实体小米手机、大体积ZIP或云端实际恢复验收；不改变上面的剩余范围。

## 本机完整备份恢复演练

- 新只读快照：2026-09-06T07:07:03Z；17张业务表，1318件持有实例，2662个媒体文件。30个新增媒体下载，其余均对照新ETag校验后复用。
- 完整ZIP：`gucang-phase5-restore-drill-20260906.zip`，SHA256 `519f79b0e1c8e27a4ab08b1aa76014aa0c5656a8183463b60b3e9a37532789ca`。
- 从本地ZIP重新解包，2711个文件逐个验证SHA256和ZIP CRC；图片全部恢复到独立本机演练目录。
- gucang-test私有隔离schema实际导入17表；70批逐字段比较零差异，17表行数一致，46个业务外键及源表约束通过。
- 发现测试库仍含旧实验字段，直接LIKE测试库表会失败；演练改为使用备份内正式库的列、枚举和约束，不依赖目标现有表。
- 演练schema已清理；正式库业务写入为0。保留本机备份及演练报告，旧阶段二备份按三份保留规则移入回收站。
- 不涵盖Auth重建、应用触发器/默认值执行、云端Storage上传及整个平台灾难恢复。浏览器版本2ZIP与本机管理员完整备份格式不同，不能混称为同一恢复测试。
